# AGENTS.md

## 实施流程
在处理任何任务时，必须严格遵循以下步骤：
* **理解**：仔细阅读需求及提供的上下文，明确任务目标。如果没有提供充足的上下文，包括API接口文档、常量的含义等，严禁猜测或捏造API接口，优先从已有代码和类似项目中查找，如果找到则需要明确标注来源，没有找到则立刻停止并说明需要我提供哪些上下文。对于在上下文中提供的URL链接，必须发送网络请求获取其内容并加入上下文，对于同一网站下不同目录的内容，如果有必要也可以加入上下文，并在生成代码后给出参考的链接。在读取API文档后（包括REST和Websocket），除了未提供密钥的私有API外，必须写一段测试代码，验证对API文档的理解是否正确，测试完成后必须删除临时测试代码，可以写单元测试，也可以写main函数，总之必须执行。对于生成代码后我做出的修改，需要理解我的想法并采纳，下次生成代码时禁止改回去。
* **思考**：在编写代码前，先构思实现方案，考虑边缘情况、性能损耗以及对现有项目架构的影响。明确陈述你的假设，如果有任何不确定的地方，请提问，不要自己决定。如果对于一个问题有多种解释，请全部列出，并由我来选择，不要自己选择。如果存在更简单的方法，请说明并询问我是否采用，不要自行采用。如果我给出的方案存在不足，有更好的方案，请指出并询问我是否采用，不要自行采用。
* **实现**：按照既定的编码规范编写代码。严禁在没有获取充足上下文的情况下编写代码，如果在读取API文档后没有写测试代码验证，也视为没有获取充足的上下文。修改代码时，不要对原有代码有较大的改动，仅做出必要的修改即可，严禁改动原有逻辑，严禁重构代码。如果发现原有代码存在问题，在完成任务后指出即可，禁止自行修改。除对话中明确要求外，必须使用`superpowers`等插件并进行TDD测试驱动开发。
* **验证**：运行测试或通过脚本验证改动是否符合预期，确保无回归错误。
* **维护上下文**：完成编码等任务后，需要重新维护上下文文件，将过时内容删除，新增新内容，但不要改变上下文文件的整体结构。上下文文件包括但不限于：`AGENTS.md`、`CLAUDE.md`和`GEMINI.md`。
* **优化单元测试**：删除临时测试代码，删除冗余、重复、低价值的单元测试，仅保留关键的单元测试。
* **代码提交**：禁止将生成的代码提交到Git，直接放在工作区即可，尤其是superpowers的设计文档。

## 构建与测试
在执行任务前，请根据当前项目的技术栈使用标准指令：
* **全量构建：** 执行项目的标准构建命令，确保环境与依赖正常。将构建结果输出到`/dev/null`，或者构建完成后删除，不要把构建结果保留到项目中，严禁将构建结果提交到Git仓库中。仅允许出现函数或结构体等未使用的警告。
* **运行测试：** 在提交或建议改动前，运行全量测试以确保无回归错误。
* **局部验证：** 针对修改的模块，运行对应的单元测试或验证脚本。修改代码后，需要考虑原有的单元测试是否需要修改。

## 编码规范

### 核心原则
* **代码参考：** 参考项目中已有代码和类似项目的源代码，并保证相同的代码风格。
* **简洁清晰：** 编写易于阅读和维护的代码，避免过度设计和过度封装。如果用全局变量+全局函数就能解决问题，则无需使用面向对象。如果用50行代码就能解决问题，而你使用了200行，说明代码可能存在过度设计和过度封装，请重写。不要对已有代码有太大的改动，能改10行代码就能解决的问题，而你改了20行，请重写。如果真的需要对已有代码有较大改动才能解决我的问题，请向我提问，在我同意之后才能有较大的改动。对于不会复用的代码，无需封装为函数，直接写并补充注释就行了，严禁过度封装。
* **逻辑显式：** 逻辑流转应直观，避免使用复杂的“黑盒”逻辑。
* **代码安全：** 严禁写出不安全的代码，严禁使用`unsafe`绕过安全机制。
* **错误处理：** 必须显式处理所有潜在的异常或错误返回。
    * 错误信息应包含足够的上下文，以便快速定位问题原因。
    * 严禁无视错误或静默失败。
    * 出现错误后，需要打印必要的错误日志信息。如果是可以预期内的错误，则打印警告日志。
    * 对于无法向上传递的错误，必须打印错误日志；对于可以向上传递的错误，如有必要则补充错误上下文，即错误的文字描述。

### 命名与结构
* **语义化命名：** 变量、函数及类的命名必须准确描述其功能和意图，并尽量简短一点。
* **一致性：** 遵循项目既有的命名惯例（如驼峰式、下划线式等）和文件组织结构。
* **作用域：** 保持变量作用域尽可能小，函数职责尽可能单一。

### 文档与注释
* **自解释代码：** 优先通过清晰的命名和结构使代码自解释。
* **关键注释：** 在复杂逻辑、算法实现或非直观的权宜方案处，必须编写详细的中文注释。
* **文档注释：** 在所有函数、方法和结构体等上方，必须添加必要的简单文档注释，允许只写一行，但不能没有。

## 交互准则

* **语言偏好：** 所有的技术讨论、解释说明及回复请始终使用 **中文**。
* **回复流程：** 
    1. 在提供方案前，先简要分析当前问题或重构的动机。
    2. 给出符合上述规范的代码实现。
    3. 如果存在破坏性改动或需要用户手动执行的操作，请显著标出。
* **主动优化：** 如果在执行任务时发现性能瓶颈、安全隐患或不符合工程实践的代码，请主动指出并提供优化建议。
* **沟通风格：** 保持专业、高效，直接解决核心问题，减少不必要的寒暄。
